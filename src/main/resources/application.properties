# Database Configuration
db.host=localhost
db.port=3306
db.name=collectibles_store
db.username=root
db.password=password

# Application Configuration
app.port=4567
app.env=development
app.log.level=INFO

# Database Connection Pool
# Reduced for free MySQL instances (max 5 connections)
db.max.connections=3
db.min.connections=1
db.connection.timeout=30000

# API Configuration
api.base.path=/api
